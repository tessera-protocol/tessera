{
  "name": "tessera-sdk",
  "version": "0.1.0",
  "description": "SDK for issuing and verifying Tessera identity credentials",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npx tsx --test src/**/*.test.ts",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "identity",
    "verification",
    "verifiable-credentials",
    "zero-knowledge",
    "sybil-resistance",
    "agent-delegation",
    "semaphore"
  ],
  "author": "Guglielmo Reggio",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/tessera-protocol/tessera"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "@semaphore-protocol/identity": "^4.0.0",
    "@semaphore-protocol/group": "^4.0.0",
    "@semaphore-protocol/proof": "^4.0.0",
    "ethers": "^6.13.4"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.4.0",
    "tsx": "^4.0.0"
  }
}

{
  "name": "@tessera-protocol/sdk",
  "version": "0.1.0-alpha.1",
  "description": "Core SDK for Tessera Guard: capability-based delegation, runtime verification, and revocation for agent actions",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "test": "npx tsx --test src/**/*.test.ts",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "ai-agents",
    "authorization",
    "agent-security",
    "delegation",
    "revocation",
    "runtime-security",
    "mcp",
    "openclaw"
  ],
  "author": "Guglielmo Reggio",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/tessera-protocol/tessera"
  },
  "homepage": "https://github.com/tessera-protocol/tessera",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "@semaphore-protocol/identity": "^4.0.0",
    "@semaphore-protocol/group": "^4.0.0",
    "@semaphore-protocol/proof": "^4.0.0",
    "ethers": "^6.13.4"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.4.0",
    "tsx": "^4.0.0"
  }
}
